import React from "react";
import {View, Text, ScrollView} from 'react-native';

const HTHeader= () =>{
    return(
            <View style={{width:"100%",justifyContent:"center",alignItems:"center",height:60,backgroundColor:"red"}}>
            <Text style={{fontSize:24,color:"white"}}>YAPILACAKLAR LİSTESİ</Text>
            </View>

    );
};

export default HTHeader;
